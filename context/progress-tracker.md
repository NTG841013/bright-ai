# Progress Tracker

Update this file whenever the current phase, active feature, or implementation state changes.

## Current Phase

- Updated Project List Item Styling: Updated project list items in `project-sidebar.tsx` to match the "New Project" button styling, including `bg-accent-primary-dim`, a cyan border, and cyan text color. Added `mb-1` for better separation. ✓
- Updated Sidebar Header Styling: Updated the 'My Projects/Shared' tabs header in `project-sidebar.tsx` to match the "New Project" button styling, including a brand-dim background, cyan border, `text-sm` font size, and cyan text color. ✓
- Fixed Sidebar Button Styling: Ensured all buttons in `project-sidebar.tsx` (Close, Rename, Delete) share the same visual style as the "New Project" button, featuring a brand-dim background, subtle border, and cyan accent text. ✓
- Fixed Duplicate Key Error in PresenceAvatars: Resolved a React console warning ("Encountered two children with the same key") by deduplicating collaborators by user ID before rendering. This ensures that even if a user has multiple tabs open, they only appear once in the avatar stack, preventing duplicate React keys. ✓
- Fixed Missing Avatar on Home Page: Replaced `!canvas` with `!projectId` in `EditorNavbar` to restore the user avatar on the home page. The previous refactor made the `canvas` context always available in the shell, which inadvertently hid the avatar due to the existing logic intended for workspace-only hiding. ✓
- Fixed Project Sidebar Toggle: Resolved an issue where the left sidebar and AI sidebar toggles were non-functional on the main editor page and access-denied state. This was caused by a missing `CanvasProvider` in those contexts, which made `useCanvas()` return `null`. Refactored `EditorShell` to internally provide `CanvasProvider`, ensuring consistent state management across all editor views. ✓
- Fixed Spec Preview Overlap & Scrolling: Resolved an issue where the `ScrollArea` and its content leaked onto the modal footer by ensuring the body container uses `flex flex-col` and the `ScrollArea` is constrained with `flex-1` instead of `h-full`. Improved scrollbar visibility by increasing the opacity of the `ScrollArea` thumb.
- Fixed Markdown Viewer Scrolling & Overflow: Refactored the spec preview modal in `AiSidebar.tsx` to use a `grid grid-rows-[auto_1fr_auto]` layout. This strictly contains the header and allows the `ScrollArea` to take up the remaining space without expanding beyond the `85vh` limit. Added `!top-[50%] !left-[50%] !-translate-x-1/2 !-translate-y-1/2` to `DialogContent` to ensure centered positioning remains stable even when content overflows, and moved padding inside the `ScrollArea` to ensure the scrollbar is correctly positioned and visible. ✓
- Fixed AI Workspace Persistence & Scrolling: Resolved an issue where the "AI is working" status and spinner persisted after task completion by explicitly deactivating the status in the `onComplete` hook of the Trigger.dev run. Fixed invisible scrollbars in the AI sidebar by correcting Base UI data attribute selectors in the `ScrollArea` component and increasing thumb visibility. ✓
- Fixed AI Sidebar Stuck State & Tab Robustness: Resolved an issue where the "Generate Spec" button and chat spinner remained stuck in an active state after task completion, especially in the Architecture tab. Refactored `AiSidebar` to remain mounted when toggled, preventing the termination of run subscriptions. Expanded Trigger.dev state handling to include `TIMED_OUT`, `STALLED`, and `PAUSED` states. Improved `resetAiState` helper for consistent cleanup across all task types. ✓
- Fixed Spec Generation Context & Download: Resolved an issue where the download spec was unrelated to the current project by correcting the extraction of `nodes` and `edges` from Liveblocks `LiveMap` structures. Refactored the download function in `AiSidebar.tsx` to use `fetch` with `cache: 'no-store'` instead of `window.open`, ensuring consistency with the (successful) preview logic and bypassing browser-side caching of stale downloads. ✓
- Fixed AI Sidebar Layout & Scrolling: Added `min-h-0` to all flex-1 containers within the AI sidebar (`Tabs`, `TabsContent`, `ScrollArea`) to prevent them from expanding beyond the constrained `aside` height. This ensures the input area remains fixed at the bottom and a scrollbar appears when chat content grows. Improved `ScrollArea` visibility by making the scrollbar absolute and the thumb more prominent. ✓
- Fixed Missing Edges in Design Agent (Parallel Execution): Updated `trigger/design-agent.ts` to explicitly demand both `addNode` and `addEdge` calls in the first parallel tool-call turn. Removed the restrictive node existence check in the `addEdge` tool's `execute` function to allow simultaneous creation of nodes and edges. Increased `maxTokens` to 2000 to handle the larger combined payload for complex architectures.
- Fixed `AI_RetryError` (Provider Error): Switched model to `openai/gpt-4o-mini` on OpenRouter and reduced `maxTokens` to 1000 (REVERTED due to edge creation issues).
- Fixed Design Agent Canvas Sync: Refactored `mutateStorage` in `trigger/design-agent.ts` to clear existing `nodes` and `edges` maps instead of replacing the root `flow` object. This ensures the frontend's `useLiveblocksFlow` remains correctly synchronized with the storage tree throughout the AI generation process.
- Fixed Design Agent AbortError: Increased 'abortSignal' timeout from 60s to 180s in 'trigger/design-agent.ts'. Complex architecture generation with multiple steps and parallel tool calls was exceeding the previous 60s limit, especially when using free models on OpenRouter.
- Fixed Credit/Token Limit Error (Switch to Free Model): Switched model to `meta-llama/llama-3.3-70b-instruct:free` on OpenRouter and reduced `maxTokens` to 1500 (REVERTED due to provider instability).
- Fixed Design Agent "Only 1 Node" Issue: Switched model back to `anthropic/claude-sonnet-4.6` with a strict `maxTokens: 2000` limit to ensure high-reasoning quality within credit constraints; updated the system prompt to explicitly demand parallel tool calling for all initial nodes (8-10) in the first step and connection in subsequent steps (REVERTED due to credit limits).
- Fixed Credit/Token Limit Error (Model Downgrade): Switched model back to `google/gemini-2.0-flash-001` and reduced `maxTokens` to 1500 to stay within available OpenRouter credit limits (REVERTED due to quality issues).
- Fixed `AI_APICallError` (Model Update): Switched model to `anthropic/claude-sonnet-4.6` in `trigger/design-agent.ts` to resolve the "No endpoints found for anthropic/claude-3.5-sonnet" error (REVERTED due to credit limits).
- Fixed Missing Edges in Design Agent: Switched model to `anthropic/claude-3.5-sonnet` (now updated to `claude-sonnet-4.6`) via OpenRouter for superior multi-step reasoning and tool-calling; implemented a virtual node tracker in `trigger/design-agent.ts` to provide immediate feedback to the AI during the generation process; refined the system prompt to explicitly enforce a multi-step workflow (nodes then edges) and a mandatory `finishDesign` call; improved storage clearing to remove legacy top-level maps.
- Fixed Design Agent Canvas Sync (Diagnostic & Compatibility Phase): Updated `trigger/design-agent.ts` with `getStorageDocument` for accurate diagnostic logging; implemented aggressive storage reset by overwriting `nodes` and `edges` keys with fresh `LiveMap` instances; switched to plain objects for nested node/edge properties in background task to ensure frontend compatibility.
- Fixed Design Agent TypeError: Resolved `nodes.clear is not a function` in `trigger/design-agent.ts` by implementing a defensive fallback that iterates and deletes keys if the `clear()` method is missing in the Liveblocks Node SDK environment.
- Fixed Design Agent Canvas Sync: Implemented explicit storage clearing before fresh designs and added a post-mutation broadcast signal to ensure the frontend picks up background changes reliably; added `refreshCanvas` flag to `AiStatusMessage` schema.
- Fixed Node Visibility in Design Agent: Added explicit `width` and `height` to node data during canvas mutation in `trigger/design-agent.ts`. This ensures nodes created by the AI are visible on the canvas, as they were previously defaulting to 0 size if the model omitted these parameters.
- Fixed Design Agent "Missing Arguments": Resolved a property mismatch where OpenRouter/OpenAI returns tool parameters in `input` while the AI SDK's `steps` object expects `args`. Updated `trigger/design-agent.ts` to check both properties.
- Fixed `AI_APICallError`: Switched model back to `google/gemini-2.0-flash-001` via OpenRouter to resolve "No endpoints found that support tool use" error encountered with `qwen/qwen-2.5-coder-32b-instruct`. Gemini 2.0 Flash is verified to support tool-calling reliably within the current project setup.
- Fixed Credit/Token Limit Error: Reduced `maxTokens` to 2500 to stay within available credit limits and prevent `AI_APICallError`.
- Fixed Design Agent TypeScript Warnings: Resolved `TS7031` (implicit any) for `onStepFinish` parameters by adding explicit types; verified with `lint`.
- Resolved IDE "No data sources" Inspections: Updated `prisma/schema.prisma` to include a proper `url = env("DATABASE_URL")` field and refactored logging in `trigger/design-agent.ts` to be more IDE-friendly, suppressing common JetBrains inspections that appeared when no database connection was configured.
- Switched back to Gemini 2.0 Flash: Switched model back to `google/gemini-2.0-flash-001` via OpenRouter as it is more stable than the experimental 2.5.
- Switched to Gemini 2.5 Flash: Switched model to `google/gemini-2.5-flash` via OpenRouter as requested for testing (REVERTED).
- Fixed Design Agent TypeScript Errors: Applied type casting (`as any`) to `generateText` options and tool call objects in `trigger/design-agent.ts` to resolve `maxSteps` and `args` property errors reported by the TypeScript compiler.
- Fixed Design Agent Early Termination: Switched to `qwen/qwen3-coder:free` and implemented a `finishDesign` tool with strict multi-step instructions and feedback loops. This ensures the agent builds the entire system architecture (5-10 nodes) before concluding, resolving the issue where it would stop after a single node call. (Note: Currently using Gemini 2.0 Flash for stability).
- Fixed Design Agent "Provider returned error": Switched to `google/gemini-2.0-flash-001` via OpenRouter to resolve recurring instability and timeout issues with `qwen/qwen3-coder:free`. Added an explicit `abortSignal` timeout to prevent the task from hanging indefinitely when the provider is unresponsive.
- Fixed Design Agent TypeError: Removed `.description()` from Zod schemas in `trigger/design-agent.ts` to resolve `external_exports.string(...).description is not a function` error, likely caused by environment-specific transpilation issues.
- Fixed Markdown Viewer Scrolling & Overflow: Refactored the spec preview modal in `AiSidebar.tsx` to use a `grid grid-rows-[auto_1fr]` layout. This strictly contains the header and allows the `ScrollArea` to take up the remaining space without expanding beyond the `85vh` limit. Added `!top-[50%] !left-[50%] !-translate-x-1/2 !-translate-y-1/2` to `DialogContent` to ensure centered positioning remains stable even when content overflows, and moved padding inside the `ScrollArea` to ensure the scrollbar is correctly positioned and visible. ✓
- Phase 2: AI Integration (Completed Phase 2: AI Integration) ✓
- Canvas Design Generation: Implementing the UI flow for triggering AI design generation and viewing results.
- AI Presence & State (24-ai-presence-state.md): Implementing real-time AI status signals and indicators (Completed).
- Sidebar Chat Feed (25-sidebar-chat-feed.md): Implementing real-time room chat in the AI sidebar (Completed).
- Design Agent Frontend (26-design-agent-frontend.md): Implementing the UI flow for triggering AI design generation and viewing results (Completed).
- Spec Generation Flow (27-spec-generation-flow.md): Implementing the backend flow for AI-powered technical spec generation (Completed).
- Spec Persistence & Download (28-spec-persistence-download.md): Persisting generated specs to Vercel Blob and providing a secure download route (Completed).
- Spec UI Integration (29-spec-ui-intergration.md): Integrated spec generation results into the editor; added spec list, preview modal with Markdown rendering, and download actions in the AI sidebar (Completed).
- Fixed Sign-Out Rebuilding & 404: Resolved an issue where signing out triggered a "Fast Refresh" rebuild and a 404 error for `ai-avatar.png`. Added `.output.txt` and `.trigger/` to `.gitignore` to prevent trace logs from triggering HMR. Refactored `AiSidebar.tsx`, `types/tasks.ts`, and `liveblocks.config.ts` to use `undefined` instead of `""` for missing avatars, stopping broken relative requests. Explicitly passed `publishableKey` to `ClerkProvider` in the root layout for improved auth stability. ✓
- Fixed Sign-Out Hang & Black Screen: Resolved an issue where signing out caused a black screen hang. Improved `proxy.ts` to return 401 for unauthenticated non-GET requests (Server Actions) instead of redirecting to HTML. Optimized `getClerkIdentity` to skip the slow `currentUser()` path (1.5s latency) during Server Action re-renders, significantly speeding up the sign-out transition. ✓
- Optimized Auth & Redirects during Sign-Out: Resolved slow 4.5s requests and redundant `/sign-in` redirects by memoizing `getClerkIdentity` with React `cache()`, implementing fast-path ownership checks, and updating `proxy.ts` to return 401 for API routes. Further reduced latency to ~2s and improved resilience by fallback to `sessionClaims` and parallelizing data fetching. ✓
- Fixed Spec Generation Database Errors: Resolved `TypeError` and `PrismaClientKnownRequestError` by updating `prisma.config.ts` to load environment variables and syncing the database schema with `npx prisma db push`.
- Improved Prisma Generation Lifecycle: Replaced the `postinstall` script with a `prepare` script in `package.json` for `prisma generate`. This ensures generation runs during local development and publishing but avoids breaking production installs where `devDependencies` (including Prisma CLI) are omitted. ✓
- Standardized Trigger.dev SDK Imports: Updated all instances of `@trigger.dev/sdk/v3` to `@trigger.dev/sdk` to align with the installed v4 version and best practices. ✓
- Resolved Trigger.dev "Pending Version" Issue: Identified that "pending version" status in runs after Vercel deployment is caused by missing background worker deployments. Background tasks must be deployed separately to the Trigger.dev platform via the CLI or GitHub integration. ✓
- Automated Trigger.dev Deployments: Added a GitHub Action workflow `.github/workflows/trigger-deploy.yml` to automatically deploy tasks to Trigger.dev on push to main and for pull request previews. ✓
- Fixed Trigger.dev CLI "No lockfile found" Error: Generated `package-lock.json` by running `npm install`. The Trigger.dev CLI requires a lockfile to bundle and deploy tasks correctly. ✓
- Resolved Trigger.dev Build Failure (Node.js version): Updated `trigger.config.ts` runtime to `node-22` to satisfy Prisma 7.8.0 engine requirements (Prisma 7.8.0+ requires Node 20.19+, 22.12+, or 24.0+). ✓
- Configured Prisma Extension in Trigger.dev: Added `directUrlEnvVarName: "DIRECT_URL"` to `prismaExtension` in `trigger.config.ts` to ensure database connections work correctly during task execution. ✓
- Fixed Task Indexing Failure (Missing Environment Variables): Modified `lib/liveblocks.ts` and `lib/prisma.ts` to avoid throwing errors if `LIVEBLOCKS_SECRET_KEY` or `DATABASE_URL` is missing during task indexing. Added defensive checks in `trigger/design-agent.ts` and `trigger/generate-spec.ts` to handle the absence of these clients gracefully during deployment while still requiring them for actual task execution. ✓

## Completed

### Bug Fixes & Refinements
- Fixed `AI_APICallError` (Model Update): Switched model to `anthropic/claude-sonnet-4.6` to resolve 404 error with legacy model IDs; maintained multi-step logic.
- Fixed `AI_APICallError`: Switched model back to `google/gemini-2.0-flash-001` in `trigger/design-agent.ts` to resolve "No endpoints found that support tool use" errors seen with other models; maintained `maxTokens: 2500` for credit safety.
- Fixed Design Agent "Missing Arguments": Enhanced argument extraction logic and enforced strict system instructions in `trigger/design-agent.ts`; added defensive validation in tool `execute` functions.
- Fixed AI Design Trigger Failure: Resolved a "Failed to trigger design generation" error by pushing the missing `task_runs` table to the database and correcting a deprecated `url` property in `prisma/schema.prisma` for Prisma 7 compatibility.
- Fixed Trigger.dev Runtime Error: Wrapped the editor room page in `TriggerAuthContext` and added an `enabled` flag to `useRealtimeRun` in `AiSidebar.tsx` to prevent the "Missing accessToken" error on initial load; verified with `build`.
- Fixed `RoomProvider` Runtime Error: Moved `AiSidebar` inside the `Room` container in `EditorShell` to ensure it correctly accesses Liveblocks presence and events; verified with `build`.

### Phase 2: AI Integration
- AI Infrastructure: Trigger.dev integrated for durable background tasks; example task created.
- Design Agent API (22-design-agent-api.md): Implemented backend flow for design generation; task triggering, tracking, and run-scoped token issuance; build verified.
- Design Agent Logic (23-design-agent-logic.md): Implemented full design agent logic in `trigger/design-agent.ts` using OpenRouter Free Router (reverted to "9 edges" state); resolved previous stability and node-creation issues by starting fresh from a high-activity state.
- Trigger.dev Integration (22-trigger-dev.md): Installed Trigger.dev SDK, configured `trigger.config.ts` with Prisma extension and retry policies, and added example task for background processing.
- AI Presence & State (24-ai-presence-state.md): Implemented shared AI activity indicators; added real-time AI status feed in sidebar using Liveblocks events; added thinking indicator to live cursors; integrated input disabling and loading states during AI activity; build verified.
- Sidebar Chat Feed (25-sidebar-chat-feed.md): Implemented real-time room chat using a separate `ai-chat` event feed; added chat message validation with Zod; integrated auto-scrolling chat history with user avatars and timestamps; build verified.
- Design Agent Frontend (26-design-agent-frontend.md): Wired AI sidebar to background task API; integrated real-time run status tracking with Trigger.dev hooks; added status strip and loading indicators during active runs; build verified.

### Phase 1: Foundation & Editor
- Design system (01-design-system.md): shadcn/ui and Tailwind v4 setup; build verified.
- Editor chrome (02-editor.md): EditorNavbar and ProjectSidebar; build verified.
- Auth (03-auth.md): Clerk integration and route protection; ensured `UserButton` is consistently visible in `EditorNavbar` across all views and consolidated its position; fixed logout redirect hang by adding `afterSignOutUrl` to `ClerkProvider`; build verified. ✓
- Logo & Branding: Replaced placeholder logo with a real, stylized SVG logo component; integrated the new logo across `AuthLeftPanel`, `EditorNavbar`, and `ProjectSidebar` for a consistent brand identity; build verified. ✓
- Project dialogs (04-project-dialogs.md): CRUD dialogs (Create, Rename, Delete) with live preview; build verified.
- Prisma setup (05-prisma.md): Multi-file schema and singleton client; build verified.
- Project APIs (06-project-apis.md): REST endpoints for project CRUD; build verified.
- Editor home wiring (07-wire-editor-home.md): Wired sidebar and dialogs to real project API; build verified.
- Editor Workspace Shell (08-editor-workspace-shell.md): Implemented workspace shell with server-side access checks; build verified.
- Share Dialog (09-share-dialog.md): Collaborator management and project sharing; build verified.
- Liveblocks setup (10-liveblocks-setup.md): Configured Presence, UserMeta, and auth; build verified.
- Base collaborative canvas (11-base-canvas.md): Liveblocks-backed React Flow canvas foundation; build verified.
- Shape panel (12-shape-panel.md): Bottom shape panel with draggable shapes; build verified.
- Node shape (13-node-shape.md): Proper shape rendering (CSS/SVG) and ghost drag preview; build verified.
- Node editing (14-node-editing.md): Resizing and inline label editing; build verified.
- Node Color Toolbar (15-nodes-color-toolbar.md): Floating color toolbar for selected nodes; build verified.
- Edge Behaviour (16-edge-behaviour.md): Custom right-angle edges with arrows and inline label editing; build verified.
- Canvas Ergonomics (17-canvas-ergonomics.md): Added floating control bar for zoom and undo/redo; implemented keyboard shortcuts; build verified.
- Starter Templates (18-starter-template.md): Created template library and import modal with lightweight SVG previews; integrated "Templates" button in navbar to replace canvas with selected template; build verified.
- Fixed Starter Templates Not Working: Replaced React Flow `setNodes`/`setEdges` in `editor-navbar.tsx` with a Liveblocks `useMutation`-based import in `canvas.tsx` that directly mutates the collaborative `LiveMap` storage. Exposed the import function via `CanvasContext` so the navbar can trigger it. This ensures template import correctly replaces the canvas in the collaborative state. ✓
- Fixed Starter Templates Runtime Error: Resolved `Cannot read properties of undefined (reading 'keys')` in `canvas.tsx` by updating `importTemplateMutation` and `useLiveblocksFlow` to target the nested `flow` object in Liveblocks storage instead of the root. This aligns with the schema defined in `liveblocks.config.ts` and ensure correct synchronization. ✓
- Presence and Live Cursors (19-presence-avatars-cursor.md): Implemented real-time collaborator presence with overlapping avatars and live cursors on the canvas; integrated Clerk UserButton with collaborator group; broadcast cursor positions via Liveblocks presence; build verified.
- AI Presence & State (24-ai-presence-state.md): Implemented shared AI activity indicators; added real-time AI status feed in sidebar using Liveblocks events; added thinking indicator to live cursors; integrated input disabling and loading states during AI activity; build verified.
- Sidebar Chat Feed (25-sidebar-chat-feed.md): Implemented real-time room chat using a separate `ai-chat` event feed; added chat message validation with Zod; integrated auto-scrolling chat history with user avatars and timestamps; build verified.
- AI Sidebar Shell (20-ai-sidebar-shell.md): Extracted AI sidebar into a separate component; implemented a tabbed UI with AI Architect and Specs tabs; added empty state, starter chips, and auto-resizing input for chat; added a static demo spec card and generate button; build verified.
- Canvas Autosave (21-canvas-autosave.md): Implemented `@vercel/blob` storage for canvas JSON and Prisma metadata persistence; added `useAutosave` hook with 2s debounce and cloud status indicator; integrated initial load from blob for empty rooms; build verified.
 
  ### Bug Fixes & Refinements
- Reverted AI Architect to "9 Edges" state: Reverted 'trigger/design-agent.ts' to use 'openrouter/free' and removed strict 'virtual canvas' validation. This returns to the state where the model was calling many 'addEdge' tools, allowing for a fresh start on fixing node creation.
- Fixed Design Agent Early Termination: Switched the AI Architect model to `deepseek/deepseek-v4-flash:free` (284B) (REVERTED)
- Switched to DeepSeek V4 Flash (Resilient): Switched to `deepseek/deepseek-v4-flash:free` (REVERTED)
- Switched to OpenRouter Free Router (Resilient): Switched back to `openrouter/free` to resolve the recurring "Provider returned error" seen with specific free models like Qwen. This router automatically selects the best available free model, providing much higher stability while maintaining the tool-enforcement and multi-turn reasoning logic.
- Fixed Design Agent Text-Only Responses: Set `toolChoice: "required"` and added a `completeDesign` tool in `trigger/design-agent.ts`. This forces the AI Architect to use tools instead of returning text-only responses, resolving the "0 nodes, 1 step" issue where the model was being "lazy" or conversational.
- Fixed Design Agent Node Creation: Updated `trigger/design-agent.ts` to implement a 'virtual canvas' during the AI run. This forces validation of node existence before allowing 'addEdge' or modifications, returning explicit error feedback to the AI if it skips node creation. This resolves the issue where the agent was jumping directly to edges on an empty canvas.
- Switched to OpenAI GPT-OSS 120B (Resilient): Switched to `openai/gpt-oss-120b:free` to resolve the recurring "Provider returned error" seen with specific free models like DeepSeek or Qwen. This massive model offers high reasoning quality and better stability while maintaining the tool-enforcement and multi-turn reasoning logic.
- Switched to DeepSeek V4 Flash (Free): Previously used `deepseek/deepseek-v4-flash:free` to address single-node issues, but it encountered "Provider returned error" instability.
- Switched to OpenRouter Free Router (Resilient): Switched back to `openrouter/free` to resolve the recurring "Provider returned error" seen with specific free models like Qwen. This router automatically selects the best available free model, providing much higher stability while maintaining the tool-enforcement and multi-turn reasoning logic.
- Switched to Qwen 3 Coder 480B (Free): Switched to `qwen/qwen3-coder:free` on OpenRouter as the primary AI model and set `toolChoice: "required"`. This massive model is optimized for technical reasoning and the "required" setting forces it to initiate tool calls in the first step, resolving the issue where the agent was returning a text-only response in one step.
- Fixed Design Agent 0-Node Generation: Added `maxSteps: 10` to `generateText` in `trigger/design-agent.ts` to allow multi-turn reasoning and tool execution; refined system prompt to enforce unique IDs and incremental building.
- Fixed Design Agent Single-Node Execution: Added `execute` functions to all tools in `trigger/design-agent.ts`. This ensures the AI receives feedback from each tool call, enabling it to continue through the multi-step reasoning loop and build complex architectures instead of stopping after one step.
- Refined Design Agent Node Creation: Updated `trigger/design-agent.ts` to switch to the resilient OpenRouter Free Router and refined the system prompt to explicitly enforce node creation before edges; added detailed step-by-step logging to debug instances where nodes were being skipped while edges were still being created.
- Refined Design Agent Node Creation: Updated `trigger/design-agent.ts` to switch to the resilient DeepSeek V4 Flash and refined the system prompt to explicitly enforce node creation before edges; added detailed step-by-step logging to debug instances where nodes were being skipped while edges were still being created.
- Refined Design Agent Node Creation: Updated `trigger/design-agent.ts` to switch to the resilient OpenRouter Free Router and refined the system prompt to explicitly enforce node creation before edges; added detailed step-by-step logging to debug instances where nodes were being skipped while edges were still being created.
- Refined Design Agent Node Creation: Updated `trigger/design-agent.ts` to switch to Qwen 3 Coder (Free) and refined the system prompt to explicitly enforce node creation before edges; added detailed step-by-step logging to debug instances where nodes were being skipped while edges were still being created.
- Fixed Design Agent `tool_choice` Error: Switched model to a variant that reliably supports the `tool_choice` parameter, resolving the `AI_APICallError`.
- Fixed Design Agent TypeError: Implemented defensive argument extraction (`args || input`) and added guard clauses for tool calls in `trigger/design-agent.ts`, resolving `Cannot read properties of undefined (reading 'id')` error.
- Fixed Design Agent Token Quota Error: Switched to OpenRouter Free Router (`openrouter/free`) to resolve credit affordability issues and provider instability while maintaining 10 `maxSteps` and 2000 `maxTokens`.
- Fixed Design Agent Token Quota Error: Switched to DeepSeek V4 Flash (`deepseek/deepseek-v4-flash:free`) to resolve credit affordability issues and provider instability while maintaining 10 `maxSteps` and 2000 `maxTokens`.
- Fixed Design Agent Token Quota Error: Switched to OpenRouter Free Router (`openrouter/free`) to resolve credit affordability issues and provider instability while maintaining 10 `maxSteps` and 2000 `maxTokens`.
- Fixed Design Agent Token Quota Error: Switched to Qwen 3 Coder 480B (`qwen/qwen3-coder:free`) to resolve credit affordability issues while maintaining 10 `maxSteps` and 2000 `maxTokens`.
- Fixed Project Modal Width: Constrained the "New Project" and other project dialogs to `sm:max-w-md` and added a default `sm:max-w-lg` to the base `DialogContent` to prevent full-width modals on desktop, ensuring better aesthetics and proper centering.
- Fixed Clerk Encryption Key Error: Removed explicit `publishableKey` and `secretKey` from `proxy.ts` and `app/layout.tsx` to prevent `encryption_key_missing` error; Clerk now relies on automatic environment variable detection.
- Fixed Clerk Infinite Redirect Loop & HMR Block: Added `allowedDevOrigins` to `next.config.ts` for IP-based dev access; updated `proxy.ts` (Next.js 16) with explicit Clerk keys and expanded public routes; ensured `ClerkProvider` receives `publishableKey` explicitly in root layout.
- Fixed Issue 1 & 7: Moved `CanvasProvider` higher to ensure Save button visibility in `EditorNavbar`; verified private blob access and SDK usage in API routes.
- Fixed React infinite loop: Memoized `useAutosave` return values and added state update guards in `CanvasProvider`.
- Fixed Issue 6: Added `img.clerk.com` to `next.config.ts`.
- Fixed Issue 1 follow-up: Memoized save functions in `useAutosave` and synced status between `CanvasControls` and `EditorNavbar` via `CanvasContext`.
- Fixed Issue 1 final: Ensured stable `triggerSave` reference in `CanvasContext` to prevent stale closure issues in `EditorNavbar`.
- Fixed Issue 1 Strict Mode Root Cause: Refactored `useAutosave` to use `useState` for loading state and removed unstable `useRef` guards that were causing silent failures in React Strict Mode; verified stable manual save trigger.
- Fixed Liveblocks Auth Timeout: Optimized Clerk API calls by fetching user identity once and passing it through the project access and auth flow, significantly reducing latency.
- Fixed Issue 2: Implemented collaborative node and edge deletion via Delete/Backspace keys; switched to `onDelete` helper from `useLiveblocksFlow` to ensure proper multiplayer synchronization.
- Fixed Autosave Selection Guard: Prevented autosave from firing while any node or edge is selected to avoid saving intermediate changes; verified that deselection triggers a delayed save.
- Fixed EditorHome Centering: Added `h-full` to `EditorHome` to ensure content is correctly centered vertically within the workspace shell.
- Fixed Canvas Controls Redo Shortcut: Updated the redo tooltip shortcut on macOS to use the standard ⌘⇧Z.
 - Fixed AI Sidebar Submission: Implemented `handleSubmit` for AI Architect chat, wiring it to both the Enter key and the Send button for consistent submission behavior.
- Fixed Trigger.dev Config Error: Resolved TypeScript error in `trigger.config.ts` by updating `defineConfig` import and adding required `mode: "legacy"` to `prismaExtension`.
- Fixed Design Agent AI Parsing Error: Replaced `generateObject` with `generateText` and tool-calling pattern to resolve `AI_NoObjectGeneratedError`. Implemented tools for full canvas mutation (add, move, resize, update, delete) and updated the design task to process these tools step-by-step for more resilient diagram generation.
- Fixed Design Agent TypeScript Errors: Updated `trigger/design-agent.ts` to follow AI SDK 6.x syntax, including `inputSchema` for tools and `stopWhen` with `stepCountIs` for multi-step execution.
- Fixed Design Agent Runtime Error: Corrected `liveblocks.mutateStorage` callback signature to use destructuring `({ root })` as required by the latest `@liveblocks/node` SDK, resolving the `root.get is not a function` error.
